# FRONTEND_DEV @ qwen3-coder-plus
## Czas
14.0s | 2450 znaków
## Zadanie
Napisz komponent src/components/Leaderboard.tsx. Ma wyświetlać tabelę z tymi danymi (name, xp, comment). Dodaj 'Ciebie' na listę (pobierz punkty z useGamification). Styl: dark space, border pink (walszak), font-display. WYPLUJ TYLKO KOD.
## Wynik
```tsx
import React from 'react';
import { useGamification } from '../hooks/useGamification';

const Leaderboard: React.FC = () => {
  const { xp } = useGamification();

  // Mock data - replace with real data fetching logic
  const leaderboardData = [
    { name: 'Alice', xp: 1250, comment: 'Great progress this week!' },
    { name: 'Bob', xp: 1100, comment: 'Consistent learner' },
    { name: 'Charlie', xp: 980, comment: 'New record achieved' },
    { name: 'Diana', xp: 870, comment: 'Always active' },
    { name: 'Eve', xp: 750, comment: 'Improving daily' },
    { name: 'Frank', xp: 630, comment: 'Steady growth' },
    { name: 'Grace', xp: 520, comment: 'Making progress' },
    { name: 'Henry', xp: 410, comment: 'Getting started' },
    { name: 'Ivy', xp: 300, comment: 'First steps' },
    { name: 'Jack', xp: 200, comment: 'Just joined' },
    { name: 'Ciebie', xp: xp || 0, comment: 'To jestem ja!' },
  ];

  // Sort by XP descending
  const sortedData = [...leaderboardData].sort((a, b) => b.xp - a.xp);

  return (
    <div className="bg-gray-900 text-white rounded-lg p-6 max-w-4xl mx-auto">
      <h2 className="text-3xl font-display font-bold mb-6 text-center text-pink-400">Ranking</h2>
      <div className="overflow-x-auto">
        <table className="min-w-full bg-gray-800 rounded-lg overflow-hidden">
          <thead>
            <tr className="border-b border-pink-500">
              <th className="py-3 px-4 text-left font-display text-pink-400">Miejsce</th>
              <th className="py-3 px-4 text-left font-display text-pink-400">Imię</th>
              <th className="py-3 px-4 text-left font-display text-pink-400">XP</th>
              <th className="py-3 px-4 text-left font-display text-pink-400">Komentarz</th>
            </tr>
          </thead>
          <tbody>
            {sortedData.map((entry, index) => (
              <tr 
                key={index} 
                className={`border-b border-pink-900 ${entry.name === 'Ciebie' ? 'bg-pink-900/30' : 'hover:bg-gray-700/50'}`}
              >
                <td className="py-3 px-4">{index + 1}</td>
                <td className="py-3 px-4 font-display">{entry.name}</td>
                <td className="py-3 px-4">{entry.xp}</td>
                <td className="py-3 px-4 text-gray-300">{entry.comment}</td>
              </tr>
            ))}
          </tbody>
        </table>
      </div>
    </div>
  );
};

export default Leaderboard;
```
