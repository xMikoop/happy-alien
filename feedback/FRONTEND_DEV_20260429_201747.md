# FRONTEND_DEV @ qwen3-coder-plus
## Czas
6.8s | 867 znaków
## Zadanie
Stworz plik src/components/Hero.tsx. SPECYFIKACJA: Komponent 'use client'. Importuje framer-motion (motion). Zawiera: naglowek H1 'KURWIX ACADEMY' w font-display, podtytul 'Naucz sie architektury systemowej z Walszakiem', animowany text 'Ciuralla!' ktora faluje (motion.div animate y [0,-10,0]), przycisk 'START NAUKE' stylizowany na kosmiczny (bg-quasar text-space-900 font-display text-xs px-8 py-4 rounded-full hover:scale-110 transition). Tlo: gradient radialny od space-700 do transparent. Kontener: min-h-screen flex flex-col items-center justify-center text-center gap-6 px-4. POKAZ TYLKO GOTOWY KOD.
## Wynik
```tsx
'use client';

import { motion } from 'framer-motion';

export default function Hero() {
  return (
    <div className="min-h-screen flex flex-col items-center justify-center text-center gap-6 px-4 bg-gradient-to-b from-space-700 via-transparent to-transparent">
      <h1 className="font-display text-4xl md:text-6xl">KURWIX ACADEMY</h1>
      <p className="text-lg md:text-xl">Naucz sie architektury systemowej z Walszakiem</p>
      <motion.div 
        animate={{ y: [0, -10, 0] }}
        transition={{ duration: 2, repeat: Infinity, ease: "easeInOut" }}
        className="text-2xl md:text-3xl font-bold"
      >
        Ciuralla!
      </motion.div>
      <button className="bg-quasar text-space-900 font-display text-xs px-8 py-4 rounded-full hover:scale-110 transition-transform duration-300">
        START NAUKE
      </button>
    </div>
  );
}
```
